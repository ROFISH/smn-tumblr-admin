.page-header
  %h1 Users

%p=link_to 'New User', [:new,:user], class:'btn btn-primary'

%ul
  -@users.each do |user|
    %li
      =link_to user.name, user
      =link_to 'delete', user, method: :delete, data:{confirm:'Are you sure?'}
